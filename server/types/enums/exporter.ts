import { Permissions } from "./permission.enum";
import { HttpStatusCodes } from "./httpStatusCodes.enum";
import { HttpRequestTypes } from "./httpRequestTypes.enum";
import { LogLevel } from "./logLevel.enum";
import { BullMqStatuses } from "./bullMQStatuses.enum";

export const Enums = {
    Permissions,
    HttpStatusCodes,
    HttpRequestTypes,
    LogLevel,
    BullMqStatuses
};
